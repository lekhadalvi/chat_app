// Relative paths route requests through our Next.js API Route Handler proxy
// which resolves CORS and forwards calls to User Service (5000) and Chat Service (5002).
export const USER_SERVICE_URL = "";
export const CHAT_SERVICE_URL = "";
